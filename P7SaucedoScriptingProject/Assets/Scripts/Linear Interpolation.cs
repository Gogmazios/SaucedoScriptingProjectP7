using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinearInterpolation : MonoBehaviour
{ /*
    // Start is called before the first frame update
    void Start()
    {
        //In this case we get 364
        float result = Math.Lerp(600f, 128f, 0.5f);


        Vector3 from = new Vector3(1f, 10f, 100f);
        Vector3 to = new Vector3(10f, 100f, 1000f);

        // Heres result = (8, 80,800) 
        Vector result = Vector3.Lerp(from, to, 0.80);
    }

    // Update is called once per frame
    void Update()
    {
        GetComponent<Light>().intensity = Mathf.Lerp(GetComponent<Light>().intensity, 10000f, 100f * Time.deltaTime);
    }
    
   */ 
}
